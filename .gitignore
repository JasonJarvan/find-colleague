# secrets
config.toml
.env
.env.*

# 私有数据：周报原文/映射/抽取/DB —— 运行态由 `find-colleague ingest` 重建，不入库
data/

# 内部 harness/记忆/协作文档（含 everclaw、内部 UUID/MR、人名、邮箱）——不发布
docs/

# 内部项目入口文档（含内部 URL/人名/harness 引用）——不发布
CLAUDE.md

# crawl/access 层含私域访问逻辑（经 sources.md 读真实 page/folder id）——不随仓库发布。
# cli.py 对 crawl 子命令惰性导入：公开 clone 里本文件缺失时优雅降级，不 import 崩溃。
src/find_colleague/crawl.py

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
dist/
*.egg-info/

# os/ide
.DS_Store
.idea/
.vscode/
*.swp
